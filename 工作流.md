---
title: 工作流
description: 
published: true
date: 2026-03-02T06:40:58.953Z
tags: 
editor: markdown
dateCreated: 2026-02-26T10:06:36.131Z
---

# 工作流

## 流程上下文后置处理

-   **上下文**：com.wyc.hr.wkflow.client.bp.service.LocalProcessContextPostProcessContext
-   **返回值**：java.lang.Void
-   **描述**：用于处理自定义的流程上下文处理逻辑。
-   **Code**：ProcessContextPostProcess
-   **触发时机**：在业务端，流程上下文构建之后，启动流程之前

## 流程管理

### 类型设置

#### 员工自助申请控制脚本

-   **上下文**：com.wyc.hr.wkflow.model.rule.WorkflowDimensionSettingScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：员工自助申请控制脚本
    
-   **Code**：ESS\_ENABLE\_CHECK
    
-   **触发时机**：用于执行工作流发起时进行范围控制。
    
-   **示例**：流程可见权限.groovy
    

```groovy
import com.wyc.hr.wkflow.model.rule.WorkflowDimensionSettingScriptContext


def main() {
    WorkflowDimensionSettingScriptContext context = ((WorkflowDimensionSettingScriptContext) ctx)
    def employee = context.getEmployee()
    def employeeIdList = [635147, 635154]
    for (int i = 0; i < employeeIdList.size(); i++) {
        if (employeeIdList[i] == employee.id) {
            return true
        }
    }
    return false
}


```

#### 工作流适配规则

-   **上下文**：com.wyc.hr.wkflow.model.rule.WorkflowDimensionSettingScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于根据工作流类型解析对应的流程模型。
    
-   **Code**：WORKFLOW\_TYPE\_RESOLVE
    
-   **触发时机**：在业务端，执行工作流类型解析脚本
    
-   **示例**：工作流适配规则.groovy
    

```groovy
import com.wyc.hr.masterdata.client.dto.EmployeeDetailsDTO

def main(){
    EmployeeDetailsDTO employee = ctx.getEmployee()
    //员工归属
    String EmployeeAttribution = employee.currentJobInfo.getPersonnelGroupName()
    //流程名称
    String name = ""
    if(EmployeeAttribution == "a" || EmployeeAttribution == null){
        name = "调岗审批"
    }
    if(EmployeeAttribution == "b"){
        name = "调岗流程"
    }
    return name
}

```

### 扩展设置

#### 表单数据检查脚本

-   **上下文**：com.wyc.hr.wkflow.model.context.ext.validate.WorkflowValidationScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行工作流，表单数据检查脚本。
    
-   **Code**：WORKFLOW\_CONTEXT\_VALIDATION
    
-   **触发时机**：用于执行工作流，表单数据检查脚本
    
-   **示例**：表单数据检查脚本.groovy
    

```groovy
package HRM.Workflow.WORKFLOW_CONTEXT_VALIDATION

def main() {
}



// 获取上下文数据
def context = context.getContext()

// 获取表单数据
def form = context.getForm()

// 获取任务名称
def taskName = context.getTaskName()

// 获取操作
def action = context.getAction()

// 获取员工信息
def employee = context.getEmployee()

// 获取发起人信息
def initiator = context.getInitiator()

// 示例：验证上下文数据
if (context != null) {
    // 检查必要字段
    if (!context.containsKey("requiredField")) {
        throw new Exception("缺少必要字段：requiredField")
    }

    // 检查字段值
    def fieldValue = context.get("requiredField")
    if (fieldValue == null || fieldValue.toString().trim() == "") {
        throw new Exception("必要字段值不能为空：requiredField")
    }

    // 记录日志
    logger.debug("Workflow context validation script executed successfully")
    logger.debug("Task name: " + taskName)
    logger.debug("Employee: " + (employee != null ? employee.getFullName() : "null"))
}

// 验证通过，不需要返回值

```

#### 流程表单初始化

-   **上下文**：com.wyc.hr.wkflow.model.context.ext.init.WorkflowInitExtensionContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行工作流，流程表单初始化。
    
-   **Code**：WORKFLOW\_INIT\_EXTENSION
    
-   **触发时机**：用于构建工作流启动前数据初始化上下文
    
-   **示例**：流程表单初始化.groovy
    

```groovy
package com.wyc.hr.wkflow.model.context.ext.init

import com.wyc.hr.wkflow.script.WorkflowScriptContext

def main() {
    def context = ctx as WorkflowInitExtensionContext

    // 获取提交人
    def initiator = context.getInitiator()

    // 获取申请人
    def applicant = context.getApplicant()

}
```

### 表单更新

#### 表单内容更新脚本

-   **上下文**：com.wyc.hr.wkflow.script.WorkflowScriptContextImpl
    
-   **返回值**：java.lang.Void
    
-   **描述**：表单内容更新脚本。
    
-   **Code**：FORM\_CONTENT\_UPDATE
    
-   **触发时机**：每次打开流程时执行
    
-   **示例**：表单内容更新脚本.groovy
    

```groovy
import com.wyc.frw.common.exception.BusinessException

//实现main函数逻辑，返回计算结果；main函数不得删除；
//可用对象：ctx用于访问当前上下文，logger用于日志，具体请参考api文档
def main(){
    throw new BusinessException("123");
}
```

### 步骤设置

#### 流程步骤退回脚本

-   **上下文**：com.wyc.hr.wkflow.model.step.script.WorkStepBackScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行流程步骤退回脚本逻辑。
    
-   **Code**：WORKSTEP\_BACK\_SCRIPT
    
-   **触发时机**：在业务端，执行流程步骤退回脚本
    
-   **示例**：工作步骤退回脚本.groovy
    

```groovy
import com.wyc.hr.wkflow.model.step.script.WorkStepBackScriptContext

//脚本上下文是   当前步骤信息和 List<可退回步骤ID和名称>
def main() {
    WorkStepBackScriptContext context = ((WorkStepBackScriptContext) ctx)
    def currentStep = context.getCurrentStep()
    def transitionTargetSteps = context.getTransitionTargetSteps()
    if (currentStep.getName() == "直属领导审批") transitionTargetSteps.removeIf { (it.workStepName == '开始') }
    return transitionTargetSteps
}
```

-   **示例**：流程步骤退回脚本.groovy

```groovy
import com.wyc.hr.wkflow.model.step.script.WorkStepBackScriptContext

//脚本上下文是   当前步骤信息和 List<可退回步骤ID和名称>
def main() {
    WorkStepBackScriptContext context = ((WorkStepBackScriptContext) ctx)
    def currentStep = context.getCurrentStep()
    def transitionTargetSteps = context.getTransitionTargetSteps()
    if (currentStep.getName() == "直属领导审批") transitionTargetSteps.removeIf { (it.workStepName == '开始') }
    return transitionTargetSteps
}
```

#### 流程步骤前处理扩展

-   **上下文**：com.wyc.hr.wkflow.model.step.extension.WorkStepExtensionScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行流程步骤前处理扩展脚本逻辑。
    
-   **Code**：WORKSTEP\_PRE\_EXTENSION
    
-   **触发时机**：流程步骤开始前
    
-   **示例**：流程步骤前处理扩展脚本.groovy
    

```groovy
def main() {
    def context = ctx as WorkStepExtensionScriptContext
    def workStepId = context.getWorkStepId()
    def workStepName = context.getWorkStepName()
    def taskApprovalStatus = context.getTaskApprovalStatus()
    def form = context.getForm()
    def cont = context.getContext()
    def initiator = context.getInitiator()
    def employee = context.getEmployee()
    def processInstanceId = context.getProcessInstanceId()
    def externalProcessInstanceId = context.getExternalProcessInstanceId()
}
```

#### 工作步骤前后处理扩展

-   **上下文**：com.wyc.hr.wkflow.model.step.extension.WorkStepExtensionScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行工作步骤前后处理扩展脚本逻辑。
    
-   **Code**：WORKSTEP\_EXTENSION
    
-   **触发时机**：1.流程步骤开始前，2.流程步骤结束后
    
-   **示例**：工作步骤后处理扩展脚本.groovy
    

```groovy
def main() {
    def context = ctx as WorkStepExtensionScriptContext
    def workStepId = context.getWorkStepId()
    def workStepName = context.getWorkStepName()
    def taskApprovalStatus = context.getTaskApprovalStatus()
    def form = context.getForm()
    def cont = context.getContext()
    def initiator = context.getInitiator()
    def employee = context.getEmployee()
    def processInstanceId = context.getProcessInstanceId()
    def externalProcessInstanceId = context.getExternalProcessInstanceId()
}
```

#### 流程步骤后处理扩展

-   **上下文**：com.wyc.hr.wkflow.model.step.extension.WorkStepExtensionScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行流程步骤后处理扩展脚本逻辑。
    
-   **Code**：WORKSTEP\_POST\_EXTENSION
    
-   **触发时机**：流程步骤结束后
    
-   **示例**：流程步骤后处理扩展脚本.groovy
    

```groovy
def main() {
    def context = ctx as WorkStepExtensionScriptContext
    def workStepId = context.getWorkStepId()
    def workStepName = context.getWorkStepName()
    def taskApprovalStatus = context.getTaskApprovalStatus()
    def form = context.getForm()
    def cont = context.getContext()
    def initiator = context.getInitiator()
    def employee = context.getEmployee()
    def processInstanceId = context.getProcessInstanceId()
    def externalProcessInstanceId = context.getExternalProcessInstanceId()
}
```

#### 流程步骤逐级审批结束脚本

-   **上下文**：com.wyc.hr.wkflow.model.step.script.WorkStepScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行流程步骤逐级审批结束脚本逻辑。
    
-   **Code**：PROGRESSIVE\_END\_SCRIPT
    
-   **触发时机**：在业务端，执行流程步骤逐级审批结束脚本
    
-   **示例**：流程步骤后处理扩展脚本.groovy
    

```groovy
def main() {
    def context = ctx as WorkStepScriptContext
    def employee = context.getEmployee()
    def assignee = context.getAssignee()
    return true
}
```

-   **示例**：工作步骤后处理扩展脚本.groovy

```groovy
def main() {
    def context = ctx as WorkStepScriptContext
    def employee = context.getEmployee()
    def assignee = context.getAssignee()
    return true
}
```

### 流程模型

#### 流程后置处理脚本

-   **上下文**：com.wyc.hr.wkflow.script.WorkflowScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行后置处理脚本逻辑。
    
-   **Code**：EXECUTE\_POST\_PROCESS\_SCRIPT
    
-   **触发时机**：在业务端，执行流程后置处理脚本
    
-   **示例**：流程后置处理脚本.groovy
    

```groovy
package HRM.Workflow.EXECUTE_POST_PROCESS_SCRIPT

import com.wyc.hr.wkflow.script.WorkflowScriptContext


def main(){
    def context = ctx as WorkflowScriptContext
    def instance = context.getProcessInstance()
    def holder = context.getContext()
    def form = context.getForm()
    def employee = context.getEmployee()
}

```

### 权限设置

#### 自定义权限检查脚本

-   **上下文**：com.wyc.hr.wkflow.setting.PermissionCheckScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行自定义权限检查脚本逻辑。
    
-   **Code**：CUSTOMIZED\_PERMISSION\_CHECK
    
-   **触发时机**：在业务端，执行自定义权限检查脚本
    
-   **示例**：自定义权限检查脚本.groovy
    

```groovy
def main() {
    def context = ctx as PermissionCheckScriptContext
    def externalProcessInstanceId = context.getExternalProcessInstanceId()
    def currentUserId = context.getCurrentUserId()
    def currentEmployee = context.getCurrentEmployee()
    return true
}
```

### 规则定义

#### 动态角色脚本

-   **上下文**：com.wyc.hr.wkflow.script.WorkflowScriptContextImpl
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行动态角色脚本逻辑
    
-   **Code**：DYNAMIC\_ROLE\_SCRIPT
    
-   **触发时机**：在业务端，执行动态角色脚本
    
-   **示例**：动态角色脚本.groovy
    

```groovy
package HRM.Workflow.DYNAMIC_ROLE_SCRIPT

import com.wyc.hr.wkflow.script.WorkflowScriptContext

def main(){
    def context = ctx as WorkflowScriptContext;
    def employee = context.getEmployee()
    def initiator = context.getInitiator()
    def instance = context.getProcessInstance()

    def approvers = new ArrayList<String>()
    if (employee != null) {
        approvers.add("approver1")
        approvers.add("approver2")
    }
    return approvers
}

```

### 自定义流程

#### 自定义工作流更新脚本

-   **上下文**：com.wyc.hr.wkflow.customized.type.CustomizedWorkflowUpdateRequestScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行自定义工作流更新脚本逻辑。
    
-   **Code**：CUSTOMIZED\_WORKFLOW\_UPDATE
    
-   **触发时机**：在业务端，执行自定义工作流更新脚本
    
-   **示例**：自定义工作流更新脚本.groovy
    

```groovy
package HRM.Workflow.CUSTOMIZED_WORKFLOW_UPDATE

import com.wyc.hr.wkflow.customized.type.CustomizedWorkflowUpdateRequestScriptContext;

// 该方法用于更新自定义流程申请，返回更新后的上下文 Map<String, Object> context;
def updateRequest(){
    //def updatedContext = [:]
    // ...
    //return updatedContext
}

```

### 流程设置

#### 流程上下文初始化脚本

-   **上下文**：com.wyc.hr.wkflow.script.WorkflowScriptContextImpl
    
-   **返回值**：java.lang.Void
    
-   **描述**：流程上下文初始化脚本。
    
-   **Code**：WORKFLOW\_CONTEXT\_INIT
    
-   **触发时机**：1.流程启动之后执行。  
    2.流程退回到开始步骤，开始步骤重新提交之后执行
    
-   **示例**：流程上下文初始化脚本.groovy
    

```groovy
//实现main函数逻辑，返回计算结果；main函数不得删除；
//可用对象：ctx用于访问当前上下文，logger用于日志，具体请参考api文档

import com.wyc.frw.common.exception.BusinessException
import com.wyc.hr.wkflow.script.WorkflowScriptContext

/**
 * 流程启动之后执行
 */
//实现main函数逻辑，返回计算结果；main函数不得删除；
//可用对象：ctx用于访问当前上下文，logger用于日志，具体请参考api文档
def main(){
    throw new BusinessException("456");
}

/**
 * 流程退回到开始步骤，开始步骤重新提交之后执行
 */
def onProcessRestarted() {

}

```

### 异常处理

#### 流程启动异常处理脚本

-   **上下文**：com.wyc.hr.wkflow.setting.StartProcessExceptionScriptContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于执行流程启动异常处理脚本逻辑。
    
-   **Code**：START\_PROCESS\_EXCEPTION\_HANDLER
    
-   **触发时机**：在业务端，执行流程启动异常处理脚本
    
-   **示例**：流程启动异常处理脚本.groovy
    

```groovy
def main() {
    def context = ctx as StartProcessExceptionScriptContext
    def externalProcessInstanceId = context.getExternalProcessInstanceId()
    def exception = context.getException()
    def currentEmployee = context.getCurrentEmployee()
    def initiatorUserId = context.getInitiatorUserId()
}
```

## 工作流

### 流程实例

#### 流程上下文后置处理

-   **上下文**：com.wyc.hr.wkflow.client.bp.service.LocalProcessContextPostProcessContext
    
-   **返回值**：java.lang.Void
    
-   **描述**：用于处理自定义的流程上下文处理逻辑。
    
-   **Code**：PROCESS\_CONTEXT\_POST\_PROCESS
    
-   **触发时机**：在业务端，流程上下文构建之后，启动流程之前
    
-   **示例**：流程上下文后置处理演示.groovy
    

```groovy
package demo.script.process.contextPostProcess

import com.wyc.hr.wkflow.client.bp.service.LocalProcessContextPostProcessContextImpl

/**
 * 演示如何在业务系统完成流程上下文构建之后：自定义工作流更新脚本.groovy
 * 1、扩展业务约束检查逻辑
 * 2、扩展业务逻辑完成数据收集
 */
def main() {
    def scriptCtx = ctx as LocalProcessContextPostProcessContextImpl;
    def context = scriptCtx.getProcessContext();

}

```